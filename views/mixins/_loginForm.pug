mixin loginForm()
  form.form(action="/login" method="POST")
    h2 접속해서 이런저런 좋은 생각 나누세요❣️
    label(for="email") 이메일
    input(type="email" name="email")
    label(for="password") 패스워드
    input(type="password" name="password")
    input.button(type="submit" value="Log in ➡️")