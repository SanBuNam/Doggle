mixin sideMenu(menu)
  .sideMenu
    nav.verticalTags
      each item in menu
        li.tag
          a.tag__link(href=item.slug, class=(currentPath.endsWith(item.slug) ? 'tag__link--active' : ''))
            span.tag__text #{item.icon} #{item.title}
