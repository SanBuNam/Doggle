mixin sideMenu(menu)
  .sideMenu
    nav.verticalTags
      each item in menu
        li.tag
          a.tag__link3(href=item.slug, class=(currentPath.endsWith(item.slug) ? 'tag__link3--active' : ''))
            span.tag__text #{item.icon} #{item.title}
