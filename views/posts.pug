extends layout

include mixins/_postCard
include mixins/_pagination

block content
  .inner
    h2= title
    .posts
      each post in posts
        +postCard(post)
    +pagination(page, pages, count)